export const unitStyle = {
  color: 'var(--text-secondary)',
  fontSize: 11,
  padding: '0 6px',
  lineHeight: '30px',
} as const;

/** Small, muted text used for inline hints, results and progress messages. */
export const secondaryTextStyle = {
  fontSize: 11,
  color: 'var(--text-secondary)',
} as const;
